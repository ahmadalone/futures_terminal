[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests that need testnet API keys
    gui: marks GUI tests (requires display)